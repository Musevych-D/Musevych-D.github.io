MykolaMainacad
Значит.Домашка
1. создаём секцию в которой находится 4 блока(квадрата). Выравнивает их по центру секции по вертикали и по горизонтали(использовать только марджини, падинги, флоат).
2. Для каждого следующего задания нужна структура из п.1(копируем её, меняем только имя секции) и выполняем задания(секций столько же, сколько и пунктов)
- при наведении на секцию меняем фоновый цвет у 2,4 блока у 1,3 появляется рамка
-при наведении на первый блок, у второго меняется фоновый цвет
- при наведении на 2 блок фон меняется у 3,4
3. Создаём новую секцию добавляем позишн релейтив. Блоки позиционируем с помощью абсолюта по примеру предыдущих заданий
При наведении на секцию:
- 1,3 меняются местами
-2,4 меняются местами
- при наведении на первый меняются местами 2,3
Обращения к элементам строить исключительно с помощью СПЕЦ СЕЛЕКТОРОВ и ПСЕВДОКЛАССОВ. В этом задании не должно быть ниодного класса или айди.